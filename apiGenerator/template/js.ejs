/**
* Create by <%=author%> on <%=createData%>
*/
'use strict'
import api from '@/utils/request'
<% urls.forEach( urlSchema => {%>
/**
  * <%=urlSchema.description%>
*/
export function <%=urlSchema.functionName%>(<% if (urlSchema.isReplace) { %><%=urlSchema.pathParams%>, <%}%>params) {
    return api.<%=urlSchema.method%>(<% if (urlSchema.isReplace) { %><%=urlSchema.url%><% } else { %>'<%=urlSchema.url%>'<%}%>, <% if (urlSchema.method === 'post') {%>params<%} else {%>{params: params}<%}%>)
}
<%} ) %>
