'use strict'

export default [
<% fetchArr.forEach( (apiObj, i) => {%>
  {
    url: '<%=apiObj.url%>',
    type: '<%=apiObj.type%>',
    response: () => {
      return {
        code: 200,
        "data": {
          "token": "12344567"
          },
        "msg": "成功"
      }
    }
  }<% if (i !== fetchArr.length - 1) { %>,<%}%><% })%>
]
