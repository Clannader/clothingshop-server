'use strict'
<% modules.forEach( moduleName => {%>
import <%=moduleName%> from './<%=moduleName%>'<%} ) %>

export default [
<% modules.forEach( (moduleName, i) => {%>
   ...<%=moduleName%><% if (i !== modules.length - 1) { %>,<%}%><%} ) %>
]
